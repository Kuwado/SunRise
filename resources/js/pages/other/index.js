import Login from './Login';
import Register from './Register';
import Landing from './Landing';

const other = {
    login: Login,
    register: Register,
    landing: Landing,
};

export default other;
